{
  "id": "human-doc-review",
  "name": "Revue documentaire",
  "version": "1.0.0",
  "category": "human",
  "kind": "action",
  "visibility": "orchestrator-only",
  "interaction": "human",
  "description": "Pause le flow pour valider UN mail (1 run = 1 mail). La file d'attente (liste) est sur la page de revue. Configurez le contexte de détail ici, puis créez la page par IA.",
  "canvas": {
    "icon": "flow-node.svg",
    "iconEmoji": "📝",
    "color": "#ea580c",
    "borderRadius": 12,
    "width": 220,
    "inputs": [
      { "id": "main", "label": "Entrée", "type": "main", "maxConnections": 1 }
    ],
    "outputs": [
      { "id": "main", "label": "Validé", "type": "main" }
    ]
  },
  "operations": {
    "review.pause": {
      "label": "Demander validation humaine",
      "description": "Met le run en waiting_human et expose une page de revue.",
      "configSchema": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "title": "Titre de la page",
            "default": "Revue documentaire"
          },
          "instructions": {
            "type": "string",
            "title": "Instructions affichées à l'humain",
            "format": "textarea",
            "default": "Vérifiez le contenu, modifiez si besoin, puis validez ou rejetez."
          },
          "reviewContext": {
            "type": "string",
            "title": "Contexte du détail (1 mail)",
            "format": "textarea",
            "default": "",
            "description": "Ce que l'humain contrôle SUR CE mail (la liste est la file d'attente). Ex. vérifier la facture PDF avant suppression."
          },
          "templateNamespace": {
            "type": "string",
            "title": "Template (avancé)",
            "default": "agent:review:invoice",
            "description": "Namespace page HTML ; généré automatiquement par le bouton IA"
          }
        }
      },
      "outputMessage": {
        "type": "human-review-result",
        "fields": [
          { "key": "decision", "label": "Décision (approve/reject)" },
          { "key": "editedText", "label": "Texte validé" },
          { "key": "editedHtml", "label": "HTML validé" },
          { "key": "attachments", "label": "Pièces jointes" },
          { "key": "sourceRef", "label": "UID / référence source" }
        ]
      }
    }
  },
  "permissions": ["entity-data"],
  "serviceRef": "backend/core/agent-flow/bricks/human-doc-review"
}
